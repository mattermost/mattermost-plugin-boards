# Руководство по автоматическому обновлению плагина

## 🎯 Обзор

После настройки системы релизов, ваш плагин Mattermost Boards поддерживает автоматическое обновление через встроенный интерфейс Mattermost.

---

## ✅ Как это работает

### 1. Проверка обновлений

Mattermost периодически проверяет наличие новых версий плагина:

- **Источник информации:** `release_notes_url` из `plugin.json`
- **URL:** `https://github.com/fambear/mattermost-plugin-boards/releases`
- **Частота проверки:** Настраивается в System Console

### 2. Определение новой версии

Mattermost сравнивает:
- Текущую версию плагина (из установленного `plugin.json`)
- Последнюю версию в GitHub Releases

Если версия в GitHub выше → доступно обновление

### 3. Скачивание и установка

При нажатии "Update":
1. Mattermost скачивает файл `boards-{version}.tar.gz` из GitHub Release
2. Распаковывает архив
3. Заменяет файлы плагина
4. Перезагружает плагин (hot reload, без перезапуска сервера)

---

## 🔧 Настройка автоматического обновления

### Шаг 1: Проверьте plugin.json

Убедитесь, что в `plugin.json` указаны правильные URL:

```json
{
  "id": "focalboard",
  "version": "9.2.2",
  "homepage_url": "https://github.com/fambear/mattermost-plugin-boards",
  "support_url": "https://github.com/fambear/mattermost-plugin-boards/issues",
  "release_notes_url": "https://github.com/fambear/mattermost-plugin-boards/releases"
}
```

**Важно:** `release_notes_url` должен указывать на ваш репозиторий!

### Шаг 2: Включите автоматическую проверку (опционально)

В Mattermost System Console:

1. Перейдите в **System Console → Plugins → Plugin Management**
2. Найдите настройку **"Enable Automatic Prepackaged Plugins"**
3. Включите автоматическую проверку обновлений

### Шаг 3: Создайте релиз

Следуйте обычному процессу создания релиза:

```bash
# 1. Обновите версию
vim plugin.json  # Измените "version": "9.2.3"

# 2. Закоммитьте и запустите релиз
git add plugin.json
git commit -m "Release v9.2.3"
make trigger-release
```

---

## 🚀 Использование автоматического обновления

### Вариант A: Ручная проверка обновлений

1. Откройте **System Console → Plugins → Plugin Management**
2. Найдите плагин **"Mattermost Boards"**
3. Нажмите **"Check for updates"**
4. Если доступна новая версия, появится кнопка **"Update"**
5. Нажмите **"Update"** для установки

### Вариант B: Автоматическая проверка

Если включена автоматическая проверка:
- Mattermost сам проверяет обновления
- В Plugin Management появится уведомление о доступном обновлении
- Нажмите "Update" для установки

---

## 📦 Требования к релизу

Для работы автоматического обновления релиз должен содержать:

### ✅ Обязательно

1. **Правильное имя файла:** `boards-{version}.tar.gz`
   - Пример: `boards-9.2.3.tar.gz`

2. **Правильная структура архива:**
   ```
   boards/
   ├── plugin.json
   ├── server/
   │   └── dist/
   │       ├── plugin-linux-amd64
   │       ├── plugin-linux-arm64
   │       ├── plugin-darwin-amd64
   │       ├── plugin-darwin-arm64
   │       └── plugin-windows-amd64.exe
   └── webapp/
       └── dist/
           └── main.js
   ```

3. **Все платформы в одном архиве:**
   - Linux (amd64, arm64)
   - macOS (amd64, arm64)
   - Windows (amd64)

### ✅ Наш workflow делает это автоматически

GitHub Actions workflow (`.github/workflows/release.yml`) создает правильный bundle:
- ✅ Собирает все платформы
- ✅ Создает правильную структуру
- ✅ Называет файл корректно
- ✅ Публикует в GitHub Releases

---

## 🔍 Проверка работы

### Тест 1: Проверка структуры релиза

```bash
# Скачайте релиз
wget https://github.com/fambear/mattermost-plugin-boards/releases/download/v9.2.3/boards-9.2.3.tar.gz

# Проверьте содержимое
tar -tzf boards-9.2.3.tar.gz | head -20

# Должны увидеть:
# boards/
# boards/plugin.json
# boards/server/dist/plugin-linux-amd64
# boards/server/dist/plugin-darwin-amd64
# и т.д.
```

### Тест 2: Проверка через Mattermost UI

1. Установите текущую версию плагина
2. Создайте новый релиз с увеличенной версией
3. В System Console нажмите "Check for updates"
4. Должна появиться кнопка "Update"

---

## ⚠️ Важные замечания

1. **Версия должна увеличиваться:**
   - Mattermost сравнивает версии семантически
   - `9.2.3` > `9.2.2` → обновление доступно
   - `9.2.2` = `9.2.2` → обновление не требуется

2. **Bundle должен содержать все платформы:**
   - Если в архиве нет бинарника для платформы сервера → установка не удастся
   - Наш workflow собирает все платформы автоматически

3. **GitHub Releases должны быть публичными:**
   - Mattermost должен иметь доступ к вашему репозиторию
   - Для приватных репозиториев нужна дополнительная настройка

---

## 🎉 Готово!

Теперь ваш плагин поддерживает автоматическое обновление через Mattermost UI!

### Процесс обновления:

1. Вы создаете новый релиз → GitHub Actions собирает bundle
2. Mattermost проверяет обновления → находит новую версию
3. Администратор нажимает "Update" → плагин обновляется автоматически
4. Готово! 🚀

---

**Дополнительная информация:**
- [RELEASE.md](../RELEASE.md) - Полная инструкция по релизам
- [QUICKSTART-RELEASE.md](../QUICKSTART-RELEASE.md) - Быстрый старт
- [Mattermost Plugin Documentation](https://developers.mattermost.com/integrate/plugins/)

