# Пример файла с переменными окружения для разработки
# Скопируйте этот файл в .env и настройте под свои нужды

# Режим отладки - собирает только для текущей платформы
# Установите в true для ускорения сборки во время разработки
MM_DEBUG=true

# Путь к серверу Mattermost для локальной разработки
# По умолчанию: ../mattermost
MM_SERVER_PATH=../mattermost

# URL сервера Mattermost для live-watch режима
MM_SERVICESETTINGS_SITEURL=http://localhost:8065

# Номер сборки (используется в релизах)
# Если не установлен, используется "dev"
# BUILD_NUMBER=1

# Путь к mattermost-utilities (опционально)
# MM_UTILITIES_DIR=../mattermost-utilities

# Порт для отладчика dlv
# DLV_DEBUG_PORT=2346

# Теги сборки для Go
# BUILD_TAGS=json1 sqlite3

# Флаги для тестов Go
# GO_TEST_FLAGS=-race

# Переменные для релиза (автоматически устанавливаются в CI)
# GITHUB_TOKEN=your_github_token_here
# GITHUB_REPOSITORY=fambear/mattermost-plugin-boards

# Настройки для update-plugin-on-server.sh
# MATTERMOST_PATH=/opt/mattermost
# PLUGIN_NAME=boards

